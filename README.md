# MySQL on Kubernetes cluster 

## String connection for local dev env 

jdbc:mysql://${DB_HOST:localhost:3306}/algafood?createDatabaseIfNotExist=true&verifyServerCertificate=false&connectionTimeZone=UTC
